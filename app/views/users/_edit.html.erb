<!-- Edit partial that renders the form partial displaying the user information 
and allows for editing the user's info and updating the same.-->
<%= form_tag(user_path,:method => :put, :class => "edit_info") do%>
<%= render 'users/form'%>
<% end %>