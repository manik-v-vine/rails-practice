<h1>Welcome to rails</h1>
Controller Data:<%= @controllerMessage%><br>
<br>
<% if false %>
<%= link_to 'Add new User', :controller => :user, :action => :new %>
<% end %>
<br><br>
<% if false%>
<% hello = "world" %>
<%= localvariable%>
 <% end %>

<h1>The User records are </h1>
 <table>
 	<tr>
 		<th>First Name</th>
 		<th>Last  Name</th>
		 <th>Email</th>
		 <th>D.O.B</th>
		 <th>Phone no.</th>
		 <th>Mobile no</th>
	</tr>
 <% @users.each do |user|%>
 <tr>
	 <td><%= user.first_name%></td>
	 <td><%= user.last_name%></td>
	 <td><%= user.email%></td>
	 <td><%= user.date_of_birth %></td>
	 <td><%= user.phone_no%></td>
	 <td><%= user.mobile_no%></td>
 </tr>
 <% end %>
 </table>


<%= form_for :nil, :url => :user_new do |f|%>
<%= f.submit "Add User"%>
<% end %>